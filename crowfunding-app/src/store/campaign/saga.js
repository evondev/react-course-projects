import { takeLatest, takeEvery } from "redux-saga/effects";
// import handleGetNews from "./handlers";
// import { getNews } from "./slice";

export default function* campaignSaga() {
  // yield takeLatest(getNews.type, handleGetNews);
}
